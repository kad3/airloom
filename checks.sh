#!/bin/bash
# This script runs the check modules and must be updated if a new one is added. Be sure to run them appropriately if they are not shell scripts.

./modules/CHECK_HTTP.sh
./modules/CHECK_MAIL.sh

